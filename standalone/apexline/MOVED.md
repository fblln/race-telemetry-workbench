# Moved

Apexline now lives in its own repository:

```text
/Users/fabio/Workspace/apexline
```

Treat this directory as a legacy snapshot kept only for migration reference.
New Apexline code, packaging, tests, and documentation should be updated in the
standalone repository instead of here.
