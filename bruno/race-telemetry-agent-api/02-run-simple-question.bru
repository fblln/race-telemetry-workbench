meta {
  name: Run – simple question (no context)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/ag-ui
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Accept: text/event-stream
}

body:json {
  {
    "threadId": "{{threadId}}",
    "messages": [
      {
        "id": "msg-001",
        "role": "user",
        "content": "List the available race sessions."
      }
    ]
  }
}
