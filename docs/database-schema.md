# Database Schema

The database schema documentation is maintained in the repository under
`db/schema.md` alongside the migrations and DBML model. It covers relational
metadata tables, TimescaleDB hypertables, analytical views, and indexing choices
used by the Query API.

Keep schema changes documented there first, then reflect user-facing API impact
in the Query API and OpenAPI pages.
